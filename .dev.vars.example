ADMIN_TOKEN=dev-admin-token
API_TOKEN=dev-api-token
# Optional deployment-level default: forward original emails to a verified Email Routing destination address.
# You can keep this empty and configure forwarding later in the admin console.
FORWARD_TO=
